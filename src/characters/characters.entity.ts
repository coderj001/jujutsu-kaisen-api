type NullStr = string | null;
export class Character {
  id: number;
  name: string;
  age: string[];
  species: NullStr;
  gender: NullStr;
  status: NullStr;
  grade: NullStr;
  appearance: NullStr;
  personality: NullStr;
  abilities: NullStr;
  image_url: string;
}